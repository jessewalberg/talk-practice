# MetaToe

Pass-and-play ultimate tic tac toe — local dev MVP.

## Setup

```bash
npm install
```

## Commands

| Command | Description |
|---------|-------------|
| `npm run dev` | Start Vite dev server |
| `npm test` | Run Vitest once |
| `npm run build` | Production build |

Add `?dev=1` to the URL for the metrics inspection panel during playtests.

Design tokens and reference layout live in `design-system/`. The app imports `design-system/styles/colors_and_type.css` and ports layout CSS from `design-system/MetaToe.html`.

See [STRATEGY.md](./STRATEGY.md) and [docs/plans/2026-05-22-001-feat-metatoe-mvp-playable-plan.md](./docs/plans/2026-05-22-001-feat-metatoe-mvp-playable-plan.md).
